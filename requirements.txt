# Phase 2: Equivariant SAR Segmentation
#
# ── On Hyak (pytorch_24.12-py3.sif container) ───────────────────────────────
# Base: PyTorch 2.6.0 + CUDA 12.4, numpy 2.x provided by container.
# Install on top of container venv:  pip install -r requirements.txt
#
# ── Local CPU/MPS ────────────────────────────────────────────────────────────
# Install PyTorch first, then: pip install -r requirements.txt

# Equivariance
pymanopt
py3nj
lie-learn==0.0.2   # 0.0.2 (July 2024) is numpy 2.x compatible
escnn>=0.1.9

# ML / data stack
# NOTE: torch and numpy are provided by the container on Hyak —
#       do NOT list them here or pip will downgrade to CPU-only wheels.
#       For local installs, install torch separately before pip install -r requirements.txt.
wandb>=0.17
scikit-learn>=1.3
scikit-image>=0.21
matplotlib
seaborn
rasterio>=1.3
geopandas>=0.14
shapely>=2.0
pandas
tifffile
scipy
tqdm
requests
